name: Site Request
description: Request support for a new site / 请求支持新站点
title: "[Site] "
labels: ["site-request"]
body:
  - type: input
    id: site_name
    attributes:
      label: Site Name / 站点名称
    validations:
      required: true

  - type: input
    id: site_url
    attributes:
      label: Site URL / 站点链接
      placeholder: "https://example.com"
    validations:
      required: true

  - type: input
    id: site_type
    attributes:
      label: Site Type / 站点类型
      description: |
        Provide the content type category, such as:
        - original / 原创
        - reprint / 转载
        - light novel / 轻小说
        - manga / 漫画
        - fanfic / 同人
        - forum / 论坛

        请填写站点所属的类型，以上为示例，可自由填写。
      placeholder: "e.g. 原创 / 转载 / 轻小说"
    validations:
      required: true

  - type: textarea
    id: site_description
    attributes:
      label: Site Description / 站点描述
      description: Basic introduction to the site. / 简单介绍一下这个站点。

  - type: textarea
    id: reason
    attributes:
      label: Why should this site be added? / 希望添加该站点的理由
      description: Explain why support for this site is important. / 请说明添加此站点的原因。
    validations:
      required: true

  - type: textarea
    id: access_model
    attributes:
      label: Access Model / 内容访问模式
      description: |
        Describe how chapters can be accessed on this site.

        请描述站点的章节访问方式，例如:

        - 全部免费，无需登录
        - 部分章节收费
        - 存在会员等级 / VIP 限制
        - 需要 App 才能查看部分内容
        - 或其他访问规则
      placeholder: |
        e.g.
        - Login required for latest chapters
        - Early chapters free, latest chapters VIP-only
        - 全部章节免费，无需登录
        - 登录免费，但有部分付费章节

  - type: textarea
    id: anti_scraping
    attributes:
      label: Anti-Scraping Behavior / 反爬行为 (如有)
      description: |
        Describe any anti-scraping or access restrictions you observed (if any).

        请描述你在访问站点时看到的任何反爬或访问限制，例如：

        - 会弹出验证码
        - 会跳转到 Cloudflare 检查页面
        - 访问过快会被暂时封锁

        如果不清楚，可以留空。
      placeholder: |
        e.g.
        - Sometimes shows Cloudflare “Checking your browser...”
        - Too many requests triggers a CAPTCHA
